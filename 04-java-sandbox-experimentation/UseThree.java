// Prints the 3 command line arguments in the opposite order they were given
public class UseThree {
    public static void main (String args[]) {
        System.out.println("Hi " + args[2] + ", " + args[1] + ", and " + args[0] + ".");
    }
}
